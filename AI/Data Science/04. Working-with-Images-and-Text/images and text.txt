#6

def threshold_image(image, low, high):
    arr = np.array(image.copy())
    arr[arr <= low] = 0
    arr[arr > high] = 255
    return  Image.fromarray(arr)

#8
meals_string = menu_content[:menu_content.index("Wine by the Glass")]

#10

num_items_by_category = pd.DataFrame(meals_table.groupby(['category']).size())
mean_price_by_category = meals_table.groupby(['category']).mean()